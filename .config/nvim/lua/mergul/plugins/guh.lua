return {
	{
		"justinmk/guh.nvim",
		-- Requires Neovim 0.13+ and the GitHub `gh` CLI on PATH.
		-- Optional integration: vim-fugitive (already installed via vim-rhubarb.lua).
		cmd = "Guh", -- lazy-load on :Guh
	},
}
